<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="150dp"
    android:layout_height="85dp"
    android:layout_marginStart="@dimen/dp2"
    android:layout_marginEnd="@dimen/dp2"
    android:id="@+id/dashboard_menu_item_container"
    app:cardElevation="@dimen/dp8"
    app:cardCornerRadius="@dimen/dp16">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginTop="@dimen/dp4"
        android:orientation="vertical">

        <FrameLayout
            android:id="@+id/dashboard_menu_item_icon_bg"
            android:layout_width="45dp"
            android:layout_height="45dp"
            android:layout_gravity="center">

            <ImageView
                android:id="@+id/dashboard_menu_item_icon"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:layout_gravity="center"
                android:tint="@color/textColor"
                tools:src="@drawable/ic_info_outline_black_24dp" />

        </FrameLayout>

        <TextView
            android:id="@+id/dashboard_menu_item_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginBottom="@dimen/dp8"
            android:textColor="@color/textColor"
            android:textSize="18sp"
            tools:text="@tools:sample/cities" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>